int res;

convert(dec) : function(int) -> int {
  if (dec == 0) {
    res = 0;
  } else {
    res = (dec % 2 + 10 * convert(dec / 2));
  }
  return res;
}

main {
  int dec;
  int bin;
  dec = 10;
  bin = convert(dec);
  return 0;  
}
