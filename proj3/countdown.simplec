countdown(x) : function(int) -> int {
  int a;
  int count;
  a = x;
  while (!(a == 0)) {
  	count = a;
  	if (count != 0) {
  		a = (a - 1);
  	}
  }
  return 0;
}


main {
  return countdown(4); 
  }
