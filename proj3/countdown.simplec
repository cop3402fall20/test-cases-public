countdown(x) : function(int) -> int {
  int a;
  int b;
  int count;
  int d;
  a = x;
  b =  4;
  count = 1;
  a = (a - count);
  b = (b + count);
  a = (a + count)*(b + count);
  d = a % count;

  return 0;
}


main {
  return countdown(4); 
}
