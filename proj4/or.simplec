fizzbuzz(x) : function(int) -> int {
  int one;
  int zero;
  int res;

  one = 1;
  zero = 0;
  
  if (0 || 0) {
    res = x + 1;
  }

  if (0 || 1) {
    res = x + 2;
  }

  if (1 || 0) {
    res = x + 3;
  }

  if (1 || 1) {
    res = x - 1;
  }


  return res;

}

main {
  int x;
  int y;
  pointer<int> yooo;
  int oy;

  x = 2;
  y = 5;
  yooo = &y;
  *yooo = fizzbuzz(25);
  oy = fizzbuzz(42);
  oy = *yooo;
 
  return 100;
}
